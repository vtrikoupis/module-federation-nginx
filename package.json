{
  "version": "1.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*"
    ]
  },  
  "scripts": {
    "build": "lerna run --parallel build",
    "clean": "lerna run --parallel clean",
    "start": "lerna run --parallel dev",
    "install:parallel": "lerna run --parallel install:parallel"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "author": "its@vangel.is",
  "license": "ISC"
}
